# TaskMate Environment Configuration

# Secret key for Flask sessions (change this in production!)
SECRET_KEY=dev-secret-key-change-in-production

# Database Configuration
# Leave commented to use SQLite (default, no setup required)
# DATABASE_URL=sqlite:///instance/taskmate.db

# To use MySQL instead, uncomment and configure:
# DATABASE_URL=mysql+pymysql://username:password@localhost/database_name

# To use PostgreSQL instead:
# DATABASE_URL=postgresql://username:password@localhost/database_name
